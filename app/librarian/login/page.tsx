'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function LibrarianLogin(){const router=useRouter();const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');function submit(e:React.FormEvent){e.preventDefault();if(username==='admin'&&password==='admin'){sessionStorage.setItem('amg-librarian','true');router.push('/librarian');}else setError('Invalid librarian credentials.');}return <main className="flex min-h-screen items-center justify-center bg-slate-950 px-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-700 bg-slate-900 p-8 text-white shadow-2xl"><p className="text-sm font-bold uppercase tracking-wider text-blue-400">AMG FOUNDATION</p><h1 className="mt-3 text-3xl font-black">Librarian Desk</h1><p className="mt-2 text-slate-400">Staff sign in for learner attendance and library operations.</p><label className="mt-7 block text-sm font-bold">Username<input value={username} onChange={e=>setUsername(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-800 px-4 py-3 text-white outline-none" placeholder="Username"/></label><label className="mt-4 block text-sm font-bold">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-800 px-4 py-3 text-white outline-none" placeholder="Password"/></label>{error&&<p className="mt-4 text-sm text-red-400">{error}</p>}<button className="mt-6 w-full rounded-xl bg-blue-600 py-3 font-bold">Enter Librarian Desk</button><p className="mt-5 text-center text-xs text-slate-500">Staff demo credentials: admin / admin</p></form></main>}
