'use client';
import { useMemo, useState } from 'react';
import { Download, LogIn, LogOut, Search, Users } from 'lucide-react';
type RecordItem={name:string;id:string;className:string;status:'IN'|'OUT';checkIn:string;checkOut:string};
const initial:RecordItem[]=[{name:'Amina Otieno',id:'AMG-001',className:'Form 3',status:'IN',checkIn:'08:12',checkOut:'—'},{name:'Brian Ochieng',id:'AMG-002',className:'Form 2',status:'OUT',checkIn:'08:03',checkOut:'10:27'},{name:'Grace Akinyi',id:'AMG-003',className:'Form 4',status:'IN',checkIn:'09:18',checkOut:'—'}];
export default function LibrarianDesk(){const [records,setRecords]=useState(initial);const [query,setQuery]=useState('');const filtered=useMemo(()=>records.filter(r=>(r.name+r.id+r.className).toLowerCase().includes(query.toLowerCase())),[records,query]);function toggle(id:string){setRecords(rs=>rs.map(r=>r.id===id?{...r,status:r.status==='IN'?'OUT':'IN',checkOut:r.status==='IN'?new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}):'—'}:r));}function download(){const csv=['Learner ID,Name,Class,Status,Check In,Check Out',...records.map(r=>`${r.id},${r.name},${r.className},${r.status},${r.checkIn},${r.checkOut}`)].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=`AMG-attendance-${new Date().toISOString().slice(0,10)}.csv`;a.click();}return <main className="min-h-screen bg-slate-100 text-slate-900"><header className="border-b bg-slate-950 text-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5"><div><p className="text-xs font-bold uppercase tracking-widest text-blue-400">AMG FOUNDATION</p><h1 className="text-2xl font-black">Librarian Desk</h1></div><button onClick={()=>{sessionStorage.removeItem('amg-librarian');location.href='/portal'}} className="rounded-xl border border-slate-700 px-4 py-2 text-sm">Exit Desk</button></div></header><div className="mx-auto max-w-7xl px-6 py-8"><div className="grid gap-4 md:grid-cols-4"><Card title="Learners today" value={records.length}/><Card title="Currently inside" value={records.filter(r=>r.status==='IN').length}/><Card title="Checked out" value={records.filter(r=>r.status==='OUT').length}/><Card title="Attendance" value="100%"/></div><section className="mt-8 rounded-3xl border bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><h2 className="text-xl font-black">Learner Check-in / Check-out</h2><p className="text-sm text-slate-500">Record every learner entering or leaving the library.</p></div><button onClick={download} className="flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 font-bold text-white"><Download size={18}/> Download Attendance</button></div><div className="relative mt-6"><Search className="absolute left-4 top-3.5 text-slate-400" size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} className="w-full rounded-xl border py-3 pl-11 pr-4" placeholder="Search learner, ID or class..."/></div><div className="mt-5 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b text-slate-500"><th className="px-3 py-3">Learner</th><th className="px-3 py-3">Class</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Check in</th><th className="px-3 py-3">Check out</th><th className="px-3 py-3">Action</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id} className="border-b last:border-0"><td className="px-3 py-4"><b>{r.name}</b><div className="text-xs text-slate-400">{r.id}</div></td><td className="px-3">{r.className}</td><td className="px-3"><span className={`rounded-full px-3 py-1 text-xs font-bold ${r.status==='IN'?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-600'}`}>{r.status}</span></td><td className="px-3">{r.checkIn}</td><td className="px-3">{r.checkOut}</td><td className="px-3"><button onClick={()=>toggle(r.id)} className="flex items-center gap-2 rounded-lg border px-3 py-2 font-semibold">{r.status==='IN'?<><LogOut size={16}/> Check out</>:<><LogIn size={16}/> Check in</>}</button></td></tr>)}</tbody></table></div></section><p className="mt-6 flex items-center gap-2 text-sm text-slate-500"><Users size={16}/> Attendance is currently stored in the browser for this prototype. Supabase persistence comes next.</p></div></main>}
function Card({title,value}:{title:string;value:string|number}){return <div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{title}</p><p className="mt-2 text-3xl font-black">{value}</p></div>}
