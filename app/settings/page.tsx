'use client'; import { useState } from 'react'; import AppShell from '@/components/app-shell';
export default function Settings(){const [dark,setDark]=useState(false);const [notify,setNotify]=useState(true);return <AppShell><section className="mx-auto max-w-4xl px-5 py-8"><p className="font-semibold text-blue-600">SETTINGS</p><h1 className="text-4xl font-black">Workspace settings</h1><p className="mt-2 text-slate-500">Manage preferences and notification behaviour.</p><div className="mt-8 divide-y rounded-2xl border bg-white"><Toggle title="Dark mode" text="Use a darker interface for low-light work." value={dark} setValue={setDark}/><Toggle title="Due-date notifications" text="Notify learners before books become overdue." value={notify} setValue={setNotify}/><div className="p-6"><h2 className="font-bold">Library information</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><input defaultValue="AMG FOUNDATION Community Library" className="rounded-xl border px-4 py-3"/><input defaultValue="Nairobi, Kenya" className="rounded-xl border px-4 py-3"/></div></div></div></section></AppShell>};function Toggle({title,text,value,setValue}:{title:string;text:string;value:boolean;setValue:(v:boolean)=>void}){return <div className="flex items-center justify-between gap-6 p-6"><div><h2 className="font-bold">{title}</h2><p className="mt-1 text-sm text-slate-500">{text}</p></div><button onClick={()=>setValue(!value)} className={`h-7 w-12 rounded-full p-1 transition ${value?'bg-blue-600':'bg-slate-200'}`}><span className={`block h-5 w-5 rounded-full bg-white transition ${value?'translate-x-5':''}`}/></button></div>}
