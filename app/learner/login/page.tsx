'use client';
import Link from 'next/link';
import { useState } from 'react';
export default function LearnerLogin(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');function submit(e:React.FormEvent){e.preventDefault();if(!email||!password)return setError('Enter your email and password.');setError('Demo sign-in ready — connect Supabase Auth for real accounts.');}return <main className="flex min-h-screen items-center justify-center bg-slate-50 px-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border bg-white p-8 shadow-xl"><Link href="/portal" className="text-sm font-semibold text-blue-600">← Back to library</Link><h1 className="mt-7 text-3xl font-black">Learner Sign In</h1><p className="mt-2 text-slate-500">Access your books, resources and learning progress.</p><label className="mt-7 block text-sm font-bold">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-2 w-full rounded-xl border px-4 py-3 outline-none focus:border-blue-500" placeholder="you@example.com"/></label><label className="mt-4 block text-sm font-bold">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-2 w-full rounded-xl border px-4 py-3 outline-none focus:border-blue-500" placeholder="••••••••"/></label>{error&&<p className="mt-4 rounded-xl bg-blue-50 p-3 text-sm text-blue-700">{error}</p>}<button className="mt-6 w-full rounded-xl bg-blue-600 py-3 font-bold text-white">Sign In</button><p className="mt-5 text-center text-sm text-slate-500">New learner? <Link href="/learner/register" className="font-bold text-blue-600">Create an account</Link></p></form></main>}
